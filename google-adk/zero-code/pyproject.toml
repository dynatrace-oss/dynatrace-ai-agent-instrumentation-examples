[project]
name = "google-adk-zero-code-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=1.0.0",
    "opentelemetry-distro>=0.50b0",
    "opentelemetry-exporter-otlp-proto-http>=1.28.0",
    # Emits the SERVER span at the HTTP entry point. ADK's own spans are all
    # span.kind=internal, so without this no service is detected in SDv2.
    # `adk api_server` serves a FastAPI app (google/adk/cli/api_server.py), so
    # this instruments ADK's own server; the demo contributes no web code.
    "opentelemetry-instrumentation-fastapi>=0.50b0",
]
