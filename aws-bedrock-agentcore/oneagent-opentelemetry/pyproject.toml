[project]
name = "aws-bedrock-agentcore-opentelemetry-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "boto3",
    "fastapi",
    "uvicorn[standard]",
    "opentelemetry-api",
    "opentelemetry-sdk",
    "opentelemetry-exporter-otlp-proto-http",
]
