[project]
name = "aws-strands-oneagent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "strands-agents",
    "strands-agents-tools",
    "boto3",
    "oneagent-sdk",
    "fastapi",
    "uvicorn[standard]",
]
